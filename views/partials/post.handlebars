<div class="card">
    <div class="card-body">
        <h3 class="card-title">
            {{ post.title }}
        </h3>
        <div class="card-text">
            {{ post.content }}
        </div>
        <div class="card-footer">
            Created by user: {{post.user_id}}.
        </div>
    </div>
</div>